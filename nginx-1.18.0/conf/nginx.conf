
worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # # ========== HTTPS 主站（443）==========
    # server {
    #     listen       443 ssl http2;
    #     server_name  wbb-dev.asia;

    #     # 上传文件大小限制（默认1MB不够）
    #     client_max_body_size 20m;

    #     # SSL 证书配置
    #     ssl_certificate      ssl/wbb-dev.asia_bundle.crt;
    #     ssl_certificate_key  ssl/wbb-dev.asia.key;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers on;

    #     # 前端静态文件
    #     location / {
    #         root   html/llsp;
    #         index  index.html index.htm;
    #     }

    #     # 图片文件：先查静态资源，找不到则回源到 Spring Boot（上传目录）
    #     location /imgs/ {
    #         alias   html/llsp/imgs/;
    #         try_files $uri @spring_imgs;
    #     }
    #     location @spring_imgs {
    #         proxy_pass http://127.0.0.1:8081;
    #         proxy_set_header Host $host;
    #     }

    #     # API 代理到 Spring Boot
    #     location /api {
    #         default_type  application/json;
    #         keepalive_timeout   30s;
    #         keepalive_requests  1000;
    #         proxy_http_version 1.1;
    #         rewrite /api(/.*) $1 break;
    #         proxy_pass_request_headers on;
    #         proxy_next_upstream error timeout;
    #         # SSE 流式响应支持
    #         proxy_buffering off;
    #         proxy_cache off;
    #         proxy_read_timeout 300s;
    #         proxy_pass http://127.0.0.1:8081;
    #     }

    #     error_page   500 502 503 504  /50x.html;
    #     location = /50x.html {
    #         root   html;
    #     }
    # }

    # ========== HTTP 自动跳转 HTTPS（80）==========
    server {
        listen       80;
        server_name   _;
        return 301 https://$host$request_uri;
    }

    # ========== 本地测试端口（8080，可选）==========
    server {
        listen       8080;
        server_name  localhost;

        # 上传文件大小限制（默认1MB不够）
        client_max_body_size 20m;

        location / {
            root   html/llsp;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        location /imgs/ {
            alias   html/llsp/imgs/;
            try_files $uri @spring_imgs;
        }
        location @spring_imgs {
            proxy_pass http://127.0.0.1:8081;
            proxy_set_header Host $host;
        }

        location /api {
            default_type  application/json;
            keepalive_timeout   30s;
            keepalive_requests  1000;
            proxy_http_version 1.1;
            rewrite /api(/.*) $1 break;
            proxy_pass_request_headers on;
            proxy_next_upstream error timeout;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 300s;
            proxy_pass http://127.0.0.1:8081;
        }
    }

    upstream backend {
        server 127.0.0.1:8081 max_fails=5 fail_timeout=10s weight=1;
    }
}
